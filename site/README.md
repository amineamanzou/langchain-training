# Hugo Site

This folder contains the public documentation site for the LangChain training notes.

The site is intentionally content-first:

- `content/notes/` stores Zettelkasten-style notes.
- `content/trainings/` stores training session summaries and practice checklists.

Hugo configuration, theme selection, and GitHub Pages publishing can be added when the
content structure is ready.
